spring:
  datasource:
    url: jdbc:h2:mem:refund_helper
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true