spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:33060/url
    username: myuser
    password: mypassword
  jpa:
    hibernate:
      ddl-auto: validate

