spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/shortUrl
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none

    defer-datasource-initialization: true
